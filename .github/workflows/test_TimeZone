name: Test_Timezone
on:
  push:
    branches:
      - master
    paths:
      - '.github/workwlows/**.yml'
jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Check timezone
        run: echo $TZ
